name: base
channels:
  - conda-forge
dependencies:
  # runtime dependencies
  - python >=3.10,<3.13
  - nodejs >=16,<17
  - yarn

  # Dependencies
  - jupyterlab >=4.0.0,<5

  # Binder
  - jupyterlab-link-share=0.2
