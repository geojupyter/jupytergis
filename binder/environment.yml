name: base
channels:
  - conda-forge
dependencies:
  # runtime dependencies
  - python >=3.12
  - nodejs >=24,<25
  - yarn

  # Dependencies
  - jupyterlab >=4.5.1

  # Binder
  - jupyterlab-link-share=0.2
