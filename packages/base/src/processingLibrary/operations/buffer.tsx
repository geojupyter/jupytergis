//inputs
// -layer (type vector
// -distance (number)

//output

import geoprocessingOperationRegistry, { OperationSpecification } from "../registry";

geoprocessingOperationRegistry.add({
    id: "buffer",
    arguments={
        layer:  

    },
    template: `
    `,
}]