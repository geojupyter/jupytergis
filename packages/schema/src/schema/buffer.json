{
  "type": "object",
  "description": "Buffer",
  "title": "IBuffer",
  "required": ["InputLayer", "bufferDistance"],
  "additionalProperties": false,
  "properties": {
    "inputLayer": {
      "title": "InputLayer",
      "type": "string",
      "description": "Select an input layer for buffering"
    },
    "bufferDistance": {
      "type": "number",
      "description": "The distance used for buffering the geometry."
    },
    "projection": {
      "type": "string",
      "description": "The spatial reference system of the buffered output.",
      "default": "EPSG:4326"
    }
  }
}
