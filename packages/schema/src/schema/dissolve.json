{
  "type": "object",
  "description": "Dissolve",
  "title": "IDissolve",
  "required": ["inputLayer", "dissolveField"],
  "additionalProperties": false,
  "properties": {
    "inputLayer": {
      "type": "string",
      "description": "The input layer for the dissolve operation."
    },
    "dissolveField": {
      "type": "string",
      "description": "The field based on which geometries will be dissolved."
    },
    "projection": {
      "type": "string",
      "description": "The spatial reference system of the output.",
      "default": "EPSG:4326"
    }
  }
}
