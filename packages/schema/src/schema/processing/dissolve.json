{
  "type": "object",
  "description": "Dissolve",
  "title": "IDissolve",
  "required": ["inputLayer", "dissolveField"],
  "additionalProperties": false,
  "properties": {
    "inputLayer": {
      "type": "string",
      "description": "The input layer for the dissolve operation."
    },
    "outputLayerName": {
      "type": "string",
      "description": "The name of the output layer for the buffered geometry."
    },
    "dissolveField": {
      "type": "string",
      "description": "The field based on which geometries will be dissolved."
    }
  }
}
