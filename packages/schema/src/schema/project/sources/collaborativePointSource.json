{
  "type": "object",
  "description": "Feature store source (server-backed baseline with Ydoc overlay).",
  "title": "ICollaborativePointSource",
  "required": ["storeId"],
  "additionalProperties": false,
  "properties": {
    "storeId": {
      "type": "string",
      "description": "Stable id for the feature store (maps to PostGIS table jgis_store_<slug>).",
      "default": ""
    },
    "tileUrlTemplate": {
      "type": "string",
      "description": "MVT URL template with {z}/{x}/{y} for the live tipg baseline (usually jupytergis_core/tiles/collections/…). Prefixed with the Jupyter base URL at runtime. Append ?v= from baselineVersion for cache busting.",
      "default": ""
    },
    "pmtilesPath": {
      "type": "string",
      "description": "Legacy path/URL to a static PMTiles baseline (unused when tileUrlTemplate is set).",
      "default": ""
    },
    "baselineVersion": {
      "type": "integer",
      "description": "Monotonic baseline version; bump after each fold and append as ?v= on tile requests.",
      "default": 0,
      "minimum": 0
    },
    "projection": {
      "type": "string",
      "description": "CRS for feature geometries (stored as EPSG:4326 GeoJSON).",
      "default": "EPSG:4326"
    },
    "attribution": {
      "type": "string",
      "readOnly": true,
      "description": "Attribution for the feature store source.",
      "default": ""
    }
  }
}
