{
  "type": "object",
  "description": "MarkerSource",
  "title": "IMarkerSource",
  "required": ["feature"],
  "additionalProperties": false,
  "properties": {
    "feature": {
      "type": "object",
      "description": "Info for the marker",
      "required": ["coords"],
      "properties": {
        "coords": {
          "type": "array",
          "minItems": 2,
          "maxItems": 2,
          "items": {
            "type": "number"
          }
        }
      }
    }
  }
}
