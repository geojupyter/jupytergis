{
  "type": "object",
  "description": "OpenEOTileSource",
  "title": "IOpenEOTileSource",
  "required": ["processGraph"],
  "additionalProperties": false,
  "properties": {
    "processGraph": {
      "type": "object",
      "readOnly": true,
      "description": "The process graph for the OpenEO layer.",
      "default": {}
    },
    "serverUrl": {
      "description": "The OpenEO tile server url.",
      "type": "string"
    },
    "authBearer": {
      "description": "DEPRECATED. The bearer token is no longer persisted; the frontend keeps the live OpenEO connection in memory and requires the user to sign in each session. This field is kept only for backwards compatibility with .jGIS files saved by older versions, and is ignored.",
      "type": "string"
    }
  }
}
