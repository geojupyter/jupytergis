{
  "type": "object",
  "description": "OpenEOTileSource",
  "title": "IOpenEOTileSource",
  "required": ["processGraph", "serverUrl"],
  "additionalProperties": false,
  "properties": {
    "processGraph": {
      "type": "object",
      "readOnly": true,
      "description": "The process graph for the OpenEO layer.",
      "default": {}
    },
    "serverUrl": {
      "description": "The OpenEO tile server url.",
      "type": "string"
    },
    "authBearer": {
      "description": "The OpenEO connection identifier (session bearer token). Persisted so a connection opened from the notebook can be reused by the UI without signing in again.",
      "type": "string"
    }
  }
}
