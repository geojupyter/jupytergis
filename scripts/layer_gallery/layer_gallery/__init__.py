"""A tool to generate layer definitions for the layer gallery.

The output is a JSON file.
By default, ensures that thumbnails exist.

When run with `--thumbnails`, generates thumbnails where possible.
"""
